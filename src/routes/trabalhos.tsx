import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, BookOpen, FileText, GraduationCap, Presentation, Search } from "lucide-react";
import { ACADEMIC_SERVICES } from "@/lib/education-universe";
export const Route=createFileRoute("/trabalhos")({component:TrabalhosPage,head:()=>({meta:[{title:"Central Acadêmica — Superlativo Heureca"}]})});
const icons=[FileText,BookOpen,GraduationCap,Search,Presentation,BookOpen];
function TrabalhosPage(){return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6 sm:py-14"><section className="rounded-[2rem] bg-navy p-7 text-white shadow-card sm:p-10"><p className="text-xs font-bold uppercase tracking-[0.18em] text-gold">Central Acadêmica</p><h1 className="mt-3 font-display text-4xl font-semibold sm:text-5xl">Da ideia à apresentação.</h1><p className="mt-4 max-w-3xl text-white/75">Organização, metodologia, revisão e clareza para trabalhos acadêmicos, preservando a autoria intelectual do estudante.</p></section><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{ACADEMIC_SERVICES.map((item,i)=>{const Icon=icons[i%icons.length];return <article key={item.title} className="rounded-[1.5rem] bg-white p-5 shadow-card"><Icon className="size-6 text-magenta"/><h2 className="mt-4 font-display text-xl font-semibold text-navy">{item.title}</h2><p className="mt-2 text-sm leading-relaxed text-muted">{item.text}</p></article>})}</div><Link to="/ia" className="mt-7 inline-flex items-center gap-2 font-semibold text-magenta">Explorar apoio com Heureca AI <ArrowRight className="size-4"/></Link></div>}